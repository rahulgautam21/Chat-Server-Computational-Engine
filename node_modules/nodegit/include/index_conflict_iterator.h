// This is a generated file, modify: generate/templates/class_header.h

#ifndef GITINDEXCONFLICTITERATOR_H
#define GITINDEXCONFLICTITERATOR_H
#include <nan.h>
#include <string>
#include <queue>
#include <utility>

#include "async_baton.h"
#include "promise_completion.h"

extern "C" {
#include <git2.h>
}

#include "../include/typedefs.h"

// Forward declaration.
struct git_index_conflict_iterator {
};

using namespace node;
using namespace v8;

class GitIndexConflictIterator : public Nan::ObjectWrap {
public:

  static Nan::Persistent<Function> constructor_template;
  static void InitializeComponent (Local<v8::Object> target);
  // diagnostic count of self-freeing object instances
  static int SelfFreeingInstanceCount;
  // diagnostic count of constructed non-self-freeing object instances
  static int NonSelfFreeingConstructedCount;

  git_index_conflict_iterator *GetValue();
  void ClearValue();

  static Local<v8::Value> New(const git_index_conflict_iterator *raw, bool selfFreeing, bool shouldDuplicate = false);
  bool selfFreeing;



private:


  GitIndexConflictIterator(git_index_conflict_iterator *raw, bool selfFreeing, bool shouldDuplicate = false);
  ~GitIndexConflictIterator();

  static NAN_METHOD(JSNewFunction);
  static NAN_METHOD(GetSelfFreeingInstanceCount);
  static NAN_METHOD(GetNonSelfFreeingConstructedCount);

  git_index_conflict_iterator *raw;
};

#endif
